---
cluster:
  - "pronghorn"
form:
  - bc_num_slots
  - bc_account
  - bc_queue
  - bc_num_hours
  - num_cores
  - version
  - cuda_version
  - email
attributes:
  bc_num_slots: 1
  num_cores:
    widget: "number_field"
    label: "Number of cores"
    value: 1
    help: |
      Number of cores on node type (8 GB per core unless requesting whole
      node). Leave blank if requesting full node.
    min: 1
    max: 28
    step: 1
  version:
    widget: "select"
    label: "JupyterLab Version"
    options:
      - [ "3.1.18",  "jupyter/3.1.18" ]
  classroom: 0
  cuda_version:
    widget: "select"
    label: "CUDA Version"
    help: |
      CUDA is Nvidia's GPU-specific parallel computing framework. A GPU node
      is required to make use of this functionality. NOTE: GPU nodes are currently
      not allocated to the ondemand partition.
    options:
      - [ 
          "cuda/11.2",    "cuda/11.2",
          data-option-for-pronghorn: true,
        ]
